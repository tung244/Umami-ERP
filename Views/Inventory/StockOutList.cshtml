@using QuanLiKhoHang.Models.Entities
@{
    var items = ViewBag.Items as List<InventoryItem>;
    int page = (int)ViewBag.CurrentPage;
    int total = (int)ViewBag.TotalPages;
    string search = (string)ViewBag.SearchTerm;
}

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h4 class="card-title">@ViewBag.Title</h4>
                <div class="card-tools">
                    <a href="@Url.Action("Index", "Inventory")" class="btn btn-secondary btn-sm">
                        <i class="fas fa-arrow-left"></i> Quay lại
                    </a>
                </div>
            </div>

            <div class="card-body">
                <form method="get" class="mb-3">
                    <div class="row">
                        <div class="col-md-6">
                            <input type="text" name="search" value="@search" class="form-control" placeholder="Tìm kiếm vật phẩm..." />
                        </div>
                        <div class="col-md-2">
                            <button type="submit" class="btn btn-secondary btn-block">
                                <i class="fas fa-search"></i> Tìm kiếm
                            </button>
                        </div>
                    </div>
                </form>

                <div class="table-responsive">
                    <table class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                <th>Mã SKU</th>
                                <th>Tên vật phẩm</th>
                                <th>Vị trí lưu trữ</th>
                                <th>Tồn kho hiện tại</th>
                                <th>Đơn vị</th>
                                <th>Thao tác</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (items != null && items.Any())
                            {
                                @foreach (var item in items)
                                {
                                    <tr>
                                        <td>@(string.IsNullOrEmpty(item.Sku) ? "-" : item.Sku)</td>
                                        <td>
                                            <strong>@item.Name</strong>
                                        </td>
                                        <td>@(item.StorageLocation?.Name ?? "-")</td>
                                        <td>
                                            @if (item.CurrentQuantity <= 0)
                                            {
                                                @* ĐÃ SỬA: Thêm class bg-* và text-* *@
                                                <span class="badge bg-danger text-white">Hết hàng</span>
                                            }
                                            else if (item.CurrentQuantity <= item.ReorderLevel)
                                            {
                                                @* ĐÃ SỬA: Thêm định dạng số và class bg-warning text-dark *@
                                                <span class="badge bg-warning text-dark">@string.Format("{0:#,##0.##}", item.CurrentQuantity)</span>
                                            }
                                            else
                                            {
                                                @* ĐÃ SỬA: Thêm định dạng số và class bg-success text-white *@
                                                <span class="badge bg-success text-white">@string.Format("{0:#,##0.##}", item.CurrentQuantity)</span>
                                            }
                                        </td>
                                        <td>@item.Unit</td>
                                        <td>
                                            @if (item.CurrentQuantity > 0)
                                            {
                                                <a href="@Url.Action("StockOut", new { id = item.ItemId })" class="btn btn-sm btn-warning">
                                                    <i class="fas fa-arrow-up me-1"></i>Xuất
                                                </a>
                                            }
                                            else
                                            {
                                                <button class="btn btn-sm btn-warning" disabled>
                                                    <i class="fas fa-ban me-1"></i>Hết hàng
                                                </button>
                                            }
                                        </td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="6" class="text-center text-muted">
                                        <i class="fas fa-inbox fa-2x mb-2"></i>
                                        <br />
                                        Không có vật phẩm nào
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>

                @if (total > 1)
                {
                    <div class="d-flex justify-content-center">
                        <nav aria-label="Page navigation">
                            <ul class="pagination">
                                @if (page > 1)
                                {
                                    <li class="page-item">
                                        <a class="page-link" href="@Url.Action("StockOutList", new { page = page - 1, search })">Trước</a>
                                    </li>
                                }

                                @for (int i = Math.Max(1, page - 2); i <= Math.Min(total, page + 2); i++)
                                {
                                    <li class="page-item @(i == page ? "active" : "")">
                                        <a class="page-link" href="@Url.Action("StockOutList", new { page = i, search })">@i</a>
                                    </li>
                                }

                                @if (page < total)
                                {
                                    <li class="page-item">
                                        <a class="page-link" href="@Url.Action("StockOutList", new { page = page + 1, search })">Sau</a>
                                    </li>
                                }
                            </ul>
                        </nav>
                    </div>
                }
            </div>
        </div>
    </div>
</div>